import styled from 'styled-components';
import { COLORS, FONT_SIZE } from '../display: inline-block;
padding: 18px 0 14px;
border: none;
text-align: center;
text-decoration: none;
color: #fefefe;
cursor: pointer;
border-radius: 4px;';

export const SubText = styled.p`
  color: ${COLORS.SUB_TEXT};
  font-size: ${FONT_SIZE.BODY2};
`;